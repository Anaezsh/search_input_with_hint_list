import {
    StyleSheet,
} from 'react-native';

import {COLORS} from '../../constants';

export const styles = StyleSheet.create({
    container: {
        zIndex: 2,
    },
    item: {
        height: 30,
        paddingHorizontal: 5,
        lineHeight: 30,
    },
});
