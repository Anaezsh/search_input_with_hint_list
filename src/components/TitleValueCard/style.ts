import {
    StyleSheet,
} from 'react-native';

import {COLORS} from '../../constants';

export const styles = StyleSheet.create({
    container: {
        marginBottom: 20,
    },
    title: {
        color: COLORS.Green,
        fontSize: 16,
        textTransform: 'uppercase',
        marginBottom: 20,
    },
});
